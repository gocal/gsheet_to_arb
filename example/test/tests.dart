import 'package:test/test.dart';

void main() {
  group('arb_utils_tests', () {
    setUp(() {});

    test('findPlaceholders Test', () {
      /*
      final l10n = L10n();
      var value = l10n.numberOfSongsAvailable(2);
      expect(value, '');
      */
    });
  });
}
